"use client";
    
    return () =>{
    };
  }, []);

  return (
    <AnimatePresence>
      {loading && (
        <motion.div
          exit={{ 
          }}
          transition={{ duration: 0.8, ease: [0.43, 0.13,
          <motion.div 
            animate={{ y: [0, -12, 0] }}
            transition={{ repeat: Infinity, duration: 2.5, ease:center"
          >
                    <div class